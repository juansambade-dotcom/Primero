{
  "name": "cloudboard",
  "version": "1.0.0",
  "private": true,
  "description": "CloudBoard planning app",
  "scripts": {
    "test": "python -m unittest discover -s tests -v",
    "start": "python app/server.py"
  }
}
